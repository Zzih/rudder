<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.zzih.rudder.dao.mapper.DatasourceMapper">

    <sql id="baseSql">
        id, name, datasource_type, host, port, database_name, params, credential,
        created_by, created_at, updated_by, updated_at
    </sql>

    <select id="queryOneByName" resultType="io.github.zzih.rudder.dao.entity.Datasource">
        select <include refid="baseSql"/>
        from t_r_datasource
        where name = #{name}
        limit 1
    </select>

    <select id="countByName" resultType="long">
        select count(*)
        from t_r_datasource
        where name = #{name}
    </select>

    <select id="countByNameExcludeId" resultType="long">
        select count(*)
        from t_r_datasource
        where name = #{name}
          and id != #{excludeId}
    </select>

</mapper>
