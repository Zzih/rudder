<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.zzih.rudder.dao.mapper.QuickLinkMapper">

    <sql id="baseSql">
        id, category, name, description, icon, url, target, sort_order, enabled,
        created_by, created_at, updated_by, updated_at
    </sql>

    <select id="queryList" resultType="io.github.zzih.rudder.dao.entity.QuickLink">
        select <include refid="baseSql"/>
        from t_r_quick_link
        <where>
            <if test="category != null">
                and category = #{category}
            </if>
            <if test="onlyEnabled != null and onlyEnabled">
                and enabled = 1
            </if>
        </where>
        order by sort_order asc, id asc
    </select>

</mapper>
