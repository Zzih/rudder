<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.zzih.rudder.dao.mapper.VersionRecordMapper">

    <sql id="baseSql">
        id, resource_type, resource_code, version_no, storage_ref, provider,
        remark, created_by, created_at
    </sql>

    <select id="queryByResourceOrderByVersionNoDesc" resultType="io.github.zzih.rudder.dao.entity.VersionRecord">
        select <include refid="baseSql"/>
        from t_r_version_record
        where resource_type = #{resourceType}
          and resource_code = #{resourceCode}
        order by version_no desc
    </select>

    <select id="queryPageByResource" resultType="io.github.zzih.rudder.dao.entity.VersionRecord">
        select <include refid="baseSql"/>
        from t_r_version_record
        where resource_type = #{resourceType}
          and resource_code = #{resourceCode}
        order by version_no desc
    </select>

    <select id="queryLatestByResource" resultType="io.github.zzih.rudder.dao.entity.VersionRecord">
        select <include refid="baseSql"/>
        from t_r_version_record
        where resource_type = #{resourceType}
          and resource_code = #{resourceCode}
        order by version_no desc
        limit 1
    </select>

</mapper>
