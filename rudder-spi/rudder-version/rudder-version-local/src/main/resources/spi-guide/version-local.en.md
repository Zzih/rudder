---
description: Snapshots written directly to MySQL (default, no extra config)
---

## Local Version Storage (default)

Version snapshots are stored directly in the MySQL `t_r_version_record` table; no extra dependencies.
Suitable for default deployments and single-node / small-team setups.
